<!DOCTYPE html>
<html>
<head>
    <title>用户管理系统-修改</title>
</head>
<body>
    <form action="index.php?action=update" method="post">
        <p>姓名:
            <input type="text" name="name" value="{{user.name}}">
        </p>
        <p>性别:
            <input name="sex" type="radio" value="男" checked="{{user.sex}}=='男' ">男
            <input name="sex" type="radio" value="女" checked="{{user.sex}}=='女'">女
        </p>
        <p>年龄:
            <input type="text" name="age" value="{{user.age}}"></input>
        </p>
        <p>简介:
            <textarea name="about">{{user.about}}</textarea>
        </p>
        <input name="id" type="hidden" value="{{user.id}}">
        <input type="submit"></input>
    </form>
</body>
</html>